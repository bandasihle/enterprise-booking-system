<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
         https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <display-name>Enterprise Booking System</display-name>

    <welcome-file-list>
        <welcome-file>index.html</welcome-file>
    </welcome-file-list>

    <!-- 30-minute session, HttpOnly cookie -->
    <session-config>
        <session-timeout>30</session-timeout>
        <cookie-config>
            <http-only>true</http-only>
            <secure>false</secure>
        </cookie-config>
    </session-config>

    <!--
        Security note:
        The original security constraints targeted /pages/student/* etc —
        paths that never existed. They have been removed.

        Access control is handled manually in each Servlet:
        if session.getAttribute("userId") == null → redirect to /index.html

        This works correctly with the OTP-based custom auth flow.
    -->

</web-app>
